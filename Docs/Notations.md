<!--               NOTATIONS

- storybook
- monorepo
- theme-ui
- Prettier
- ESlint
- commitlint -> [build, chore, ci, docs, feat, fix, perf, refactor, revert, style, test]
- husky

- tests

- graphQL (backend)
- relay



 INSTALATIONS

 - storybook (ui) ->
 yarn add @storybook/addon-centered @storybook/addon-info @storybook/react babel-loader @storybook/addons @storybook/theming -D

- lerna (root) -> yarn add lerna -WD

- theme-ui (root) ->
yarn add theme-ui -W
yarn add @types/theme-ui -WD

Prettier (root) -> yarn add prettier -WD

ESlint (root) ->  yarn add eslint eslint-config-airbnb eslint-config-airbnb-base eslint-config-prettier eslint-config-react-app eslint-plugin-flowtype eslint-plugin-import eslint-plugin-jest eslint-plugin-jest-dom eslint-plugin-jsx-a11y eslint-plugin-prettier eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-relay @typescript-eslint/eslint-plugin @typescript-eslint/parser -WD

commitlint (root) -> yarn add @commitlint/cli @commitlint/config-conventional lint-staged imagemin-lint-staged -WD

husky (root) -> yarn add husky  -WD

Jest, used to make tests (root) -> yarn add jest @emotion/jest ts-jest @testing-library/jest-dom @testing-library/react -WD

  -->
